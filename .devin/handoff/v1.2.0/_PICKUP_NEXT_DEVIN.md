# READ ME FIRST

**Hi next Devin.** Before you do *anything* on the v1.2.0 batch:

1. Open [`SESSION_HANDOFF.md`](./SESSION_HANDOFF.md) and read the
   **"CURRENT PICKUP STATE"** block at the very top.
2. **v1.2.0 starts EMPTY.** Items will be added by `@alviarts` as they
   E2E-test v1.1.0 and discover bugs / paper-cuts / nice-to-haves.
3. The user owns prioritization. Do **not** invent items unilaterally.
   When the user reports a bug or feature request, append it as a new
   row in [`PROGRESS.md`](./PROGRESS.md) with status `OPEN`, write the
   spec section in [`BUGS.md`](./BUGS.md), commit + push to the
   `v120-handoff` branch, then claim it normally.
4. Follow the master prompt in `SESSION_HANDOFF.md` (TL;DR section).

If the user pasted an old master prompt by mistake (v1.0.8, v1.1.0, etc.):
- v1.0.8 batch shipped → tag `v1.0.8` on main.
- v1.1.0 batch shipped → tag `v1.1.0` on main.
- The current batch is **v1.2.0**, not those.
- Read this folder (`.devin/handoff/v1.2.0/`).

Status snapshot: 0/0 items (batch empty, awaiting user input).
